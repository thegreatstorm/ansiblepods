---
- hosts: localhost
  tasks:
  - name: "Install RustServer"
    expect:
      command: "runuser -l rustserver -c '~/./rustserver install'"
      ignore_errors: yes
      responses:
        (?i)[Y/n] : ""
